(define (reverse_ lst)
    '()
)

(define (sorted? lst)
    #t
)

(define (max_ lst)
    0
)

(define (filter_ func lst)
    '()
)

(define (max_divisible3 lst)
    0
)

(define (range k)
    '()
)

(define (pow a n)
    0
)

(define (generate_powers k)
    '()
)